#include <set>
using namespace std;
int solution(vector<int> &A) {
    set<int> Store;
    int size=A.size();
    for(int i = 0; i < size; i++)
    {
        Store.insert(A[i]);
    }
    int result=Store.size();
    return result;
}
